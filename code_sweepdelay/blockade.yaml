# blockade.yaml

containers:
  cassandra_a:
    image: cassandra:3.11             # REQUIRED so Blockade accepts this entry :contentReference[oaicite:2]{index=2}
    cap_add:
      - NET_ADMIN
    expose:
      - 9042
    start_delay: 20
    # no command or volumes needed, we’re importing the existing container

  cassandra_b:
    image: cassandra:3.11
    cap_add:
      - NET_ADMIN
    expose:
      - 9042
    start_delay: 20

  cassandra_c:
    image: cassandra:3.11
    cap_add:
      - NET_ADMIN
    expose:
      - 9042
    start_delay: 20

network:
  driver: udn                        # attach to your Docker-Compose network :contentReference[oaicite:3]{index=3}
  name: cassandra-demo_default       # the exact name of your Compose network 
  flaky: 30%                          # baseline: no packet loss :contentReference[oaicite:5]{index=5}
  slow: 75ms 100ms distribution normal  # baseline: no delay :contentReference[oaicite:6]{index=6}
